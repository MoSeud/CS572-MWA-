import { MakeItBiggerDirective } from './make-it-bigger.directive';

describe('MakeItBiggerDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeItBiggerDirective();
    expect(directive).toBeTruthy();
  });
});
